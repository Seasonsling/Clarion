{
  "name": "chuijiao-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend services for the Chuijiao application.",
  "scripts": {
    "build": "mkdir -p public && cp index.html index.css public/ && esbuild index.tsx --bundle --outfile=public/index.js --target=es2020 --jsx=automatic"
  },
  "dependencies": {
    "@google/genai": "^1.22.0",
    "@vercel/postgres": "^0.9.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/node": "^20.14.9",
    "@vercel/node": "^3.2.0",
    "esbuild": "^0.21.4",
    "typescript": "^5.5.2"
  }
}